# Frontend environment variables (used in production)
# For development, the proxy in package.json handles API requests

# Production backend API URL
REACT_APP_API_URL=https://elite-homes-api.onrender.com/api
